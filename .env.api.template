# Environment variables template for API-based VM Autoscaler
# Copy this file to .env and fill in your actual values

# Proxmox API Credentials - Username/Password Authentication
# Format: username@realm (e.g., root@pam, admin@pve)
PROXMOX_HOST1_API_PASSWORD=your_secure_password_here
PROXMOX_CLUSTER_API_PASSWORD=your_cluster_password_here

# Proxmox API Credentials - Token Authentication (Recommended)
# To create API tokens in Proxmox:
# 1. Go to Datacenter -> Permissions -> API Tokens
# 2. Add a new token (e.g., user: root@pam, token ID: autoscaler)
# 3. Set permissions: PVEVMAdmin on / (root)
PROXMOX_HOST2_API_TOKEN_ID=root@pam!autoscaler
PROXMOX_HOST2_API_TOKEN_SECRET=xxxxxxxx-xxxx-xxxx-xxxx-xxxxxxxxxxxx

# Email Configuration (Optional)
SMTP_PASSWORD=your_smtp_password

# Gotify Configuration (Optional)
GOTIFY_APP_TOKEN=your_gotify_token

# Additional API tokens for multiple hosts/clusters
# PROXMOX_HOST3_API_TOKEN_ID=user@pam!token-name
# PROXMOX_HOST3_API_TOKEN_SECRET=token-secret-here

# Custom CA Certificate path (if using self-signed certificates)
# PROXMOX_CA_CERT_PATH=/path/to/your/ca.crt

# Security Notes:
# 1. Set file permissions: chmod 600 .env
# 2. Never commit this file to version control
# 3. Use API tokens instead of passwords when possible
# 4. Regularly rotate credentials
# 5. Limit API token permissions to minimum required