# Environment variables template for VM Autoscale
# Copy this file to .env and fill in your actual values
# Make sure to set proper file permissions: chmod 600 .env

# SSH Credentials for Proxmox hosts
# Use separate variables for each host or a common one
PROXMOX_HOST1_SSH_PASSWORD=your_secure_password_here
PROXMOX_HOST2_SSH_PASSWORD=your_secure_password_here

# Email/SMTP Configuration
SMTP_PASSWORD=your_smtp_password_here

# Gotify Configuration
GOTIFY_APP_TOKEN=your_gotify_app_token_here

# Optional: Override default configuration paths
# VM_AUTOSCALE_CONFIG=/path/to/config.yaml
# VM_AUTOSCALE_LOG_FILE=/path/to/logfile.log

# Optional: SSH known_hosts file location
# SSH_KNOWN_HOSTS_FILE=/home/user/.ssh/known_hosts